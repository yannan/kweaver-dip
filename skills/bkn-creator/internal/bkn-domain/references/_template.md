# XX领域提取参考

> 复制此模板为 `domain-xxx.md`（xxx 为领域英文标识），填写后提交 PR 即可。

## 评分配置

| high (3) | mid (2) | low (1) |
|----------|---------|---------|
| 填写该领域专属词汇，如专有单据、缩写 | 填写领域常见词 | 填写弱信号词，需多个共现才有意义 |

满分：计算方式 = high词数×3 + mid词数×2 + low词数×1

## 典型对象类（canonical）

列出领域核心实体及其英文标识，格式：`中文名(标识)`

示例：
- 产品(`product`)
- 订单(`order`)
- ...

## 典型关系类

列出对象间的关系，格式：`源对象 `动词` 目标对象（基数）`

示例：
- 产品 `包含` BOM（1:N）
- 订单 `关联` 产品（N:1）

## 领域闭环补全

| 命中对象 | 缺失时补全 |
|---------|-----------|
| 示例：order | 示例：product |

填写规则：当识别到某对象时，哪些对象应该自动补全

## 关系识别提示

填写从关键词到关系类型的映射提示

示例：
- "提交、审批、流转" → 单据流转链路
- "分配、归属、负责" → 人员与对象关系

## 主键候选

列出该领域常见的主键字段名

示例：
- 编码类：xxx_code
- 单据类：xxx_no、xxx_id