name: data-catalog-build

on:
  push:
    branches:
      - main
      - master
      - github-action-build  # TODO: remove after CI migration stabilizes
    tags:
      - "v*"
    paths:
      - "dsg/services/apps/data-catalog/**"
      - ".github/workflows/data-catalog-build.yaml"
      - ".github/workflows/reusable-build-service.yml"
  pull_request:
    branches:
      - main
      - master
    paths:
      - "dsg/services/apps/data-catalog/**"
      - ".github/workflows/data-catalog-build.yaml"
      - ".github/workflows/reusable-build-service.yml"
  workflow_dispatch:
    inputs:
      push_latest:
        description: "Whether to also publish the latest tag"
        required: false
        default: "false"

jobs:
  build:
    uses: ./.github/workflows/reusable-build-service.yml
    with:
      service_name: data-catalog
      image_name: af/data-catalog
      chart_dir: dsg/services/apps/data-catalog/helm/data-catalog
      dockerfile: dsg/services/apps/data-catalog/docker/Dockerfile
      build_context: dsg
      push_latest: ${{ inputs.push_latest || 'false' }}
    secrets: inherit
