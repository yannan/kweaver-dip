name: data-subject-build

on:
  push:
    paths:
      - "dsg/services/apps/data-subject/**"
      - ".github/workflows/data-subject-build.yaml"
      - ".github/workflows/reusable-build-service.yml"
      - "VERSION"
  workflow_dispatch:
    inputs:
      push_latest:
        description: "Whether to also publish the latest tag"
        required: false
        default: "false"

jobs:
  build:
    uses: ./.github/workflows/reusable-build-service.yml
    with:
      service_name: data-subject
      image_name: af/data-subject
      chart_dir: dsg/services/apps/data-subject/helm/data-subject
      dockerfile: dsg/services/apps/data-subject/docker/Dockerfile
      build_context: dsg
      push_latest: ${{ inputs.push_latest || 'false' }}
    secrets: inherit
