name: data-exploration-service-build

on:
  push:
    paths:
      - "dsg/services/apps/data-exploration-service/**"
      - ".github/workflows/data-exploration-service-build.yaml"
      - ".github/workflows/reusable-build-service.yml"
      - "VERSION"
  workflow_dispatch:
    inputs:
      push_latest:
        description: "Whether to also publish the latest tag"
        required: false
        default: "false"

jobs:
  build:
    uses: ./.github/workflows/reusable-build-service.yml
    with:
      service_name: data-exploration-service
      image_name: af/data-exploration-service
      chart_dir: dsg/services/apps/data-exploration-service/helm/data-exploration-service
      dockerfile: dsg/services/apps/data-exploration-service/docker/Dockerfile
      build_context: dsg
      push_latest: ${{ inputs.push_latest || 'false' }}
    secrets: inherit
