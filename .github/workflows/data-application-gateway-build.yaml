name: data-application-gateway-build

on:
  push:
    paths:
      - "dsg/services/apps/data-application-gateway/**"
      - ".github/workflows/data-application-gateway-build.yaml"
      - ".github/workflows/reusable-build-service.yml"
      - "VERSION"
  workflow_dispatch:
    inputs:
      push_latest:
        description: "Whether to also publish the latest tag"
        required: false
        default: "false"

jobs:
  build:
    uses: ./.github/workflows/reusable-build-service.yml
    with:
      service_name: data-application-gateway
      image_name: af/data-application-gateway
      chart_dir: dsg/services/apps/data-application-gateway/helm/data-application-gateway
      dockerfile: dsg/services/apps/data-application-gateway/docker/Dockerfile
      build_context: dsg
      push_latest: ${{ inputs.push_latest || 'false' }}
    secrets: inherit
