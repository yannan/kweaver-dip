name: data-view-build

on:
  push:
    branches:
      - main
      - master
      - github-action-build  # TODO: remove after CI migration stabilizes
    tags:
      - "v*"
    paths:
      - "dsg/services/apps/data-view/**"
      - ".github/workflows/data-view.yml"
      - ".github/workflows/reusable-build-service.yml"
  pull_request:
    branches:
      - main
      - master
    paths:
      - "dsg/services/apps/data-view/**"
      - ".github/workflows/data-view.yml"
      - ".github/workflows/reusable-build-service.yml"
  workflow_dispatch:
    inputs:
      push_latest:
        description: "Whether to also publish the latest tag"
        required: false
        default: "false"

jobs:
  build:
    uses: ./.github/workflows/reusable-build-service.yml
    with:
      service_name: data-view
      image_name: af/data-view
      chart_dir: dsg/services/apps/data-view/helm/data-view
      dockerfile: dsg/services/apps/data-view/docker/Dockerfile
      build_context: dsg
      push_latest: ${{ inputs.push_latest || 'false' }}
    secrets: inherit
