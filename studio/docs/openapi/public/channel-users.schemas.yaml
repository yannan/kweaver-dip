components:
  schemas:
    ChannelUserType:
      type: string
      enum:
        - feishu
        - dingding

    ChannelUserChannel:
      type: object
      properties:
        type:
          $ref: '#/components/schemas/ChannelUserType'
        user_id:
          type: string
      required:
        - type
        - user_id

    ChannelUserListItem:
      type: object
      properties:
        displayName:
          type: string
        channel:
          $ref: '#/components/schemas/ChannelUserChannel'
      required:
        - displayName
        - channel

    ChannelUserListResponse:
      type: object
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/ChannelUserListItem'
        total:
          type: integer
        start:
          type: integer
        limit:
          type: integer
      required:
        - items
        - total
        - start
        - limit

    ChannelUserImportResult:
      type: object
      properties:
        count:
          type: integer
      required:
        - count

    ChannelUserImportError:
      type: object
      properties:
        line:
          type: integer
        raw:
          type: string
        reason:
          type: string
      required:
        - line
        - raw
        - reason
