migrations目录
    a. init.sql文件
        i. 保存当前版本对应的数据库初始化语句，用于初次部署时初始化表结构
        ii. 必需幂等，初始化过程中如果脚本执行失败，会重复执行
        iii. 如果一个服务使用了多个数据库，在 init.sql 中需在 create table 等操作之前通过 use xxx 指定对应的数据库
    b. 版本号目录，对应服务版本，如: 0.7.0
        i. 存放该版本引入的升级脚本；若当前版本仅新增初始化表结构、没有存量数据迁移，可只保留 init.sql
            1. 文件命名规则：“2位数字-”+“描述信息”+“.sql”
            2. 必需幂等，升级过程中如果脚本执行失败，会重复执行
